<?xml version="1.0" encoding="utf-8"?>
<!-- Reusable typography, surfaces and button styles. Colours come from Colors.xaml. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <x:Double x:Key="PurrContentMaxWidth">400</x:Double>
    <CornerRadius x:Key="PurrCardCornerRadius">24</CornerRadius>
    <CornerRadius x:Key="PurrSettingsCardCornerRadius">12</CornerRadius>

    <!-- ============================== Typography ============================== -->

    <Style x:Key="PurrTimerTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI" />
        <Setter Property="FontSize" Value="60" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="CharacterSpacing" Value="-20" />
        <Setter Property="TextLineBounds" Value="Tight" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{ThemeResource PurrTextPrimaryBrush}" />
    </Style>

    <Style x:Key="PurrSessionLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="Foreground" Value="{ThemeResource PurrAccentBrush}" />
    </Style>

    <Style x:Key="PurrMessageStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="MaxLines" Value="2" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="Foreground" Value="{ThemeResource PurrTextSecondaryBrush}" />
    </Style>

    <Style x:Key="PurrSecondaryTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource PurrTextSecondaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="PurrCaptionTextStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource PurrTextSecondaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="PurrSectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyStrongTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource PurrTextPrimaryBrush}" />
        <Setter Property="Margin" Value="4,20,0,8" />
    </Style>

    <Style x:Key="PurrSettingLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource PurrTextPrimaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- ============================== Surfaces ============================== -->

    <Style x:Key="PurrCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource PurrCardBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource PurrCardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource PurrCardCornerRadius}" />
    </Style>

    <Style x:Key="PurrSettingsCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource PurrCardBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource PurrCardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource PurrSettingsCardCornerRadius}" />
    </Style>

    <!-- A settings row: label/description on the left, control on the right. -->
    <Style x:Key="PurrSettingRowStyle" TargetType="Grid">
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="MinHeight" Value="60" />
        <Setter Property="ColumnSpacing" Value="16" />
    </Style>

    <Style x:Key="PurrDividerStyle" TargetType="Rectangle">
        <Setter Property="Height" Value="1" />
        <Setter Property="Fill" Value="{ThemeResource PurrDividerBrush}" />
    </Style>

    <!-- ============================== Buttons ============================== -->

    <!-- The one prominent action: Start / Pause / Resume. -->
    <Style x:Key="PurrPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
        <Setter Property="MinWidth" Value="136" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Padding" Value="22,0" />
        <Setter Property="CornerRadius" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

    <!-- Quiet circular icon buttons for Reset and Skip. -->
    <Style x:Key="PurrRoundIconButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="44" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="CornerRadius" Value="22" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="PurrFooterButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="CornerRadius" Value="18" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource PurrTextSecondaryBrush}" />
    </Style>

    <x:Double x:Key="PurrIconSize">16</x:Double>
</ResourceDictionary>
